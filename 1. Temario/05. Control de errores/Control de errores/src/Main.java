public class Main {
    public static void main(String[] args) {
        //Inicializo las variables

        //Declaro un array bidimensional
        int numeros[][] = {
                {10, 20, 30, 40, 50},
                {1, 2, 3, 4, 5}
        };

        System.out.println("Hn esta clase hablamos sobre controlar los errores");

        //Nombrar bien las variables.
        //Las variables tienen que estar bien nombradas, si ejecutar el códdigo ya debeemos saber que contienen esas variables.

        //Evitar no comentar el código
        //Pero tampoco hay que comentar demasiado


        //En este ejemplo creo un bucle (al que no hace falta declarar el nombre correcto de las variales
        // para recorrer el array y mostrarlo por pantalla.

        for (int i = 0; i < numeros.length; i++){
                System.out.println(numeros);
            for (int j = 0; j < numeros[j].length; j++){
                System.out.println(numeros);
            }
        }

        //las funciones también tienen que tener un buen nombre y tienen que identificar lo que hacen a simple vista

    }
}